{
  "name": "linjiang-workshop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "临江创意工坊：自定义主播、城市节点与拓展内容分享平台",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute linjiang_workshop --local --file=database/schema.sql",
    "db:remote": "wrangler d1 execute linjiang_workshop --remote --file=database/schema.sql",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/contracts.js && node --check public/app.js && node --check bridge/临江创意工坊桥接.js && npm test"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
